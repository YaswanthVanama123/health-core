@{
    ViewBag.Title = "Check Your Email";
}

<div class="form-top">
    <div class="form-top-left">
        <h3>@ViewBag.Title</h3>
        <p>@Html.ValidationSummary(true, "", new { @class = "text-danger" })</p>
    </div>
    <div class="form-top-right">
        <i class="fa fa-lock"></i>
    </div>
</div>
<div class="form-bottom">
    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        <div class="form-group">
            <div class="form-group">
                <h4 class="text-center-white">We sent the instructions for resetting your password.</h4>
                <h5 class="text-center-white"><i>Please, Check your Inbox(or Spam) folder.</i></h5>
            </div>
        </div>
        <a href="@Url.Action("Login","Account")">&lt; Back to login</a>
    }
</div>